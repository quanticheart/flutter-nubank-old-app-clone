import 'package:flutter/material.dart';

class SpaceHalf extends StatelessWidget {
  const SpaceHalf({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24,
    );
  }
}
