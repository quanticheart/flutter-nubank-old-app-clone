import 'package:flutter/material.dart';

class SpaceBig extends StatelessWidget {
  const SpaceBig({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 32,
    );
  }
}
