import 'package:flutter/material.dart';

class SpaceSmall extends StatelessWidget {
  const SpaceSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 8,
      height: 8,
    );
  }
}
